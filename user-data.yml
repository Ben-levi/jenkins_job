#cloud-config
users:
  - name: automation-user
    ssh-authorized-keys:
      - <YOUR_SSH_PUBLIC_KEY> # Replace with your SSH public key (e.g., contents of ~/.ssh/linode_key.pub)
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL'] # Passwordless sudo for automation-user
